Using the ATK GUI
=================

For ease of use, ATK also features a GUI. In terms of available features, the GUI sits somewhere between command-line and scripted usage of the package.

The GUI can be most easily launched via the command line:

.. code-block:: console

    ATKgui

The GUI should be largely self-explanatory, and includes the majority of ATK's features.
